#!/bin/sh

USERID=`id -u`
if [[ ! -e /tmp/emacs$USERID/server ]]
then
  emacs --daemon
  emacsclient -c -n -e '(switch-to-buffer "*dashboard*")'
else
  emacsclient -c -n -e '(switch-to-buffer "*dashboard*")'
fi
